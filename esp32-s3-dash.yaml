# Guition JC4827W543C: дашборд гаража в стиле exceltable.com, шаблон 002
# «KPI дашборд с ярким дизайном» (темные панели, неоновая обводка, градиент
# оранжевый -> малиновый). Живые данные гаража из HA.
#
# Это вторая прошивка для той же платы, что и esp32-s3-disp.yaml (лог-консоль
# и BLE-радар). Имя и адрес те же, чтобы запись в HA не менялась. Какая стоит
# на плате, решает то, какой файл залит последним. Сборка у обоих общая
# (.esphome/build/esp32-s3-disp), при переключении пересобирается целиком.
#
# Экран на LVGL: скругления, градиенты и свечение через тень. Шрифты
# растровые, без сглаживания (bpp 1), каждый в родном размере или ровно x2.
# Радар и график с заливкой рисуются лямбдой на холстах (скрипт draw_charts):
# штатное lvgl.canvas.draw_polygon в LVGL 9 рисует только контур.
#
# Откуда данные. Текущие значения плата берет сама (sensor/text_sensor/
# binary_sensor platform homeassistant) и раз в 2 секунды обновляет экран
# скриптом refresh, меняя только то, что изменилось. Историю (24 часа мощности
# и 12 месяцев энергии) считает HA из долгосрочной статистики и отдает
# действием set_history: автоматизация garage_dash_push_history в
# HomeAssistant/automations/misc/garage_dash.yaml, раз в 10 минут и при
# подключении платы.
#
# Что где:
#   синяя плашка   мощность гаража сейчас    sensor.schetchik_garazh_power
#   СЕГОДНЯ        розетки / свет / зарядка  sensor.garage_*_daily
#   ЗАРЯДКА АВТО   сессия ночной зарядки     sensor.garage_*_charge_session
#   полоса 23-07   ход окна зарядки          input_datetime.garage_charge_*
#   фишки          ворота, охрана, свет, движение
#   СЕНТЯБРЬ       гараж за месяц            sensor.garage_*_monthly
#   кольцевая      доля линий за месяц       sensor.garage_*_monthly
#   СЕТЬ (радар)   напряжение, частота, cos, ток (ATORCH), нагрев розетки, плата счетчика
#   КЛИМАТ         температура, влажность, давление, нагрев розетки, батарейки
#   столбики       гараж по месяцам          статистика sensor.schetchik_garazh_energy, change
#   график         мощность за сутки         статистика sensor.schetchik_garazh_power, mean
substitutions:
  board_name: 'esp32-s3-disp'
  static_ip: '192.168.0.221'

esphome:
  name: ${board_name}
  # копии столбиков через <<: *vbar переопределяют x и value, так и задумано
  merge_warnings: false
  # скриншот экрана по HTTP :8080/screen.bmp, см. комментарий в файле
  includes:
    - dash_snapshot.h
  on_boot:
    - priority: -100
      then:
        - lambda: 'dash_snap::start(id(lvgl_comp).get_disp());'
        - delay: 500ms
        - script.execute: draw_charts
        - script.execute: refresh

esp32:
  board: esp32-s3-devkitc-1
  variant: esp32s3
  flash_size: 4MB
  cpu_frequency: 240MHz
  framework:
    type: esp-idf

psram:
  mode: octal
  speed: 80MHz

wifi:
  ssid: !secret wifi
  password: !secret wifipwd
  fast_connect: true
  reboot_timeout: 3min
  manual_ip:
    static_ip: ${static_ip}
    gateway: 192.168.0.1
    subnet: 255.255.255.0
    dns1: 192.168.0.7
    dns2: 192.168.0.1
  ap:
    ssid: ${board_name}_Fallback
    password: !secret wifipwd

captive_portal:

api:
  actions:
    # История от HA: power 24 часовых средних, кВт; months 12 месяцев, кВт·ч;
    # days 30 дней, кВт·ч, последний это сегодня; first_hour местный час
    # первого часового ведра; first_dow день недели первого дня (0 = пн);
    # first_day число месяца первого дня.
    - action: set_history
      variables:
        power: float[]
        months: float[]
        days: float[]
        first_hour: int
        first_dow: int
        first_day: int
      then:
        - lambda: |-
            id(g_power) = power;
            id(g_months) = months;
            id(g_days) = days;
            id(g_first_hour) = first_hour;
            id(g_first_dow) = first_dow;
            id(g_first_day) = first_day;
            ESP_LOGI("dash", "history: %u hours, %u months, %u days", (unsigned) power.size(),
                     (unsigned) months.size(), (unsigned) days.size());
        - script.execute: draw_charts
        - script.execute: refresh

ota:
  platform: esphome
  password: !secret wifipwd

logger:
  level: INFO

# Перезагрузка в первые 15 секунд после OTA откатывает на прошлую прошивку
safe_mode:
  boot_is_good_after: 15s

button:
  - platform: safe_mode
    name: ${board_name} Safe Mode (OTA)

output:
  - platform: ledc
    pin: GPIO1
    id: backlight_pwm
    frequency: 5000Hz

light:
  - platform: monochromatic
    output: backlight_pwm
    name: ${board_name} Backlight
    id: backlight
    restore_mode: RESTORE_DEFAULT_ON

i2c:
  id: i2c_bus
  sda: GPIO8
  scl: GPIO4
  frequency: 400kHz

spi:
  id: quad_spi
  type: quad
  clk_pin: GPIO47
  data_pins: [GPIO21, GPIO48, GPIO40, GPIO39]

display:
  # Настройки панели те же, что в esp32-s3-disp.yaml: INVON, шина не выше
  # паспортных 32 МГц. Поворот на 180 при LVGL задается в блоке lvgl. Без лямбды драйвер работает без своего
  # буфера, кадры в панель пишет LVGL.
  - platform: mipi_spi
    id: lcd
    model: JC4827W543
    spi_id: quad_spi
    invert_colors: true
    data_rate: 26.67MHz
    auto_clear_enabled: false
    update_interval: never

touchscreen:
  - platform: gt911
    id: ts
    i2c_id: i2c_bus
    interrupt_pin:
      number: GPIO3
      ignore_strapping_warning: true
    reset_pin: GPIO38
    # GT911 и так отдает координаты как видит глаз, а LVGL (rotation: 180)
    # разворачивает их еще раз, как будто тач смотрит туда же, куда матрица.
    # Без зеркала тап по вкладкам внизу справа уходил в верхний левый угол.
    transform:
      mirror_x: true
      mirror_y: true
    on_touch:
      - lambda: |-
          for (auto &t : touches)
            ESP_LOGD("touch", "x=%d y=%d raw=%d,%d", t.x, t.y, t.x_raw, t.y_raw);

globals:
  - id: g_power
    type: std::vector<float>
    restore_value: no
  - id: g_months
    type: std::vector<float>
    restore_value: no
  - id: g_first_hour
    type: int
    restore_value: no
    initial_value: '0'
  - id: g_days
    type: std::vector<float>
    restore_value: no
  - id: g_first_dow
    type: int
    restore_value: no
    initial_value: '0'
  - id: g_first_day
    type: int
    restore_value: no
    initial_value: '1'
  # вид графика: 0 сутки по часам, 1 неделя по дням, 2 месяц по дням
  - id: g_view
    type: int
    restore_value: yes
    initial_value: '1'
  - id: g_radar
    type: std::vector<float>
    restore_value: no
    initial_value: 'std::vector<float>(6, 0.0f)'
  - id: g_tick
    type: int
    restore_value: no
    initial_value: '0'

time:
  - platform: homeassistant
    id: ha_time

sensor:
  - {platform: homeassistant, internal: true, id: ha_p_now, entity_id: sensor.schetchik_garazh_power}
  - {platform: homeassistant, internal: true, id: ha_volt, entity_id: sensor.schetchik_garazh_voltage}
  - {platform: homeassistant, internal: true, id: ha_freq, entity_id: sensor.schetchik_garazh_frequency}
  - {platform: homeassistant, internal: true, id: ha_pf, entity_id: sensor.schetchik_garazh_power_factor}
  - {platform: homeassistant, internal: true, id: ha_cur, entity_id: sensor.schetchik_garazh_current}
  - {platform: homeassistant, internal: true, id: ha_cpu_t, entity_id: sensor.schetchik_garazh_cpu_temperature}
  - {platform: homeassistant, internal: true, id: ha_sock_t, entity_id: sensor.rozetki_garazh_temperature}
  - {platform: homeassistant, internal: true, id: ha_p_sock, entity_id: sensor.rozetki_garazh_power}
  - {platform: homeassistant, internal: true, id: ha_p_chg, entity_id: sensor.zariadka_avto_power}
  - {platform: homeassistant, internal: true, id: ha_d_sock, entity_id: sensor.garage_sockets_daily}
  - {platform: homeassistant, internal: true, id: ha_d_light, entity_id: sensor.garage_light_daily}
  - {platform: homeassistant, internal: true, id: ha_d_chg, entity_id: sensor.garage_charger_daily}
  - {platform: homeassistant, internal: true, id: ha_m_sock, entity_id: sensor.garage_sockets_monthly}
  - {platform: homeassistant, internal: true, id: ha_m_light, entity_id: sensor.garage_light_monthly}
  - {platform: homeassistant, internal: true, id: ha_m_chg, entity_id: sensor.garage_charger_monthly}
  - {platform: homeassistant, internal: true, id: ha_s_sock, entity_id: sensor.garage_sockets_charge_session}
  - {platform: homeassistant, internal: true, id: ha_s_chg, entity_id: sensor.garage_charger_charge_session}
  - {platform: homeassistant, internal: true, id: ha_temp, entity_id: sensor.garage_air_temperature}
  - {platform: homeassistant, internal: true, id: ha_hum, entity_id: sensor.garage_humidity}
  - {platform: homeassistant, internal: true, id: ha_press, entity_id: sensor.garage_pressure}
  - {platform: homeassistant, internal: true, id: ha_bat1, entity_id: sensor.garage_climate_battery}
  - {platform: homeassistant, internal: true, id: ha_bat2, entity_id: sensor.garage_gate_battery}
  - {platform: homeassistant, internal: true, id: ha_bat3, entity_id: sensor.garage_motion_battery}

text_sensor:
  - {platform: homeassistant, internal: true, id: ha_alarm, entity_id: alarm_control_panel.shliuz_4_alarm}
  - {platform: homeassistant, internal: true, id: ha_light, entity_id: switch.svet_garazh}
  - {platform: homeassistant, internal: true, id: ha_line, entity_id: input_select.garage_charge_line}
  - {platform: homeassistant, internal: true, id: ha_chg_on, entity_id: input_boolean.garage_charge_enabled}
  - {platform: homeassistant, internal: true, id: ha_chg_start, entity_id: input_datetime.garage_charge_start}
  - {platform: homeassistant, internal: true, id: ha_chg_end, entity_id: input_datetime.garage_charge_end}

binary_sensor:
  - {platform: homeassistant, internal: true, id: ha_gate, entity_id: binary_sensor.garage_gate}
  - {platform: homeassistant, internal: true, id: ha_motion, entity_id: binary_sensor.garage_motion_any}
  # по нему HA понимает, что плата вышла на связь, и сразу шлет историю
  - platform: status
    name: Status

interval:
  - interval: 2s
    then:
      - script.execute: refresh
  - interval: 100ms
    then:
      - lambda: 'dash_snap::loop(id(lvgl_comp).get_disp());'

font:
  # Пиксельные шрифты из пака int10h (PxPlus = с кириллицей), bpp 1 без
  # сглаживания. Попадают в пиксель только в родном размере или кратном ему.
  - file: "fonts/PxPlus_HP_100LX_6x8.ttf"
    id: fs
    size: 8
    bpp: 1
    glyphs: &glyphs " !%()+,-./0123456789:;°·ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzАБВГДЕЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯабвгдежзийклмнопрстуфхцчшщъыьэюя"
  - file: "fonts/PxPlus_IBM_VGA_8x14.ttf"
    id: fm
    size: 14
    bpp: 1
    glyphs: *glyphs
  - file: "fonts/PxPlus_IBM_VGA_8x16.ttf"
    id: fb
    size: 16
    bpp: 1
    glyphs: *glyphs
  - file: "fonts/PxPlus_IBM_VGA_8x16.ttf"
    id: fx
    size: 32
    bpp: 1
    glyphs: " %.0123456789"

script:
  # Радар и график с заливкой. Заливка веером треугольников от центра
  # (радар) и трапециями из двух треугольников (график) через lv_draw_triangle.
  - id: draw_charts
    then:
      - lambda: |-
          auto line = [](lv_layer_t *l, float x1, float y1, float x2, float y2, uint32_t c, int w, lv_opa_t o) {
            lv_draw_line_dsc_t d; lv_draw_line_dsc_init(&d);
            d.p1.x = x1; d.p1.y = y1; d.p2.x = x2; d.p2.y = y2;
            d.color = lv_color_hex(c); d.width = w; d.opa = o;
            d.round_start = 1; d.round_end = 1;
            lv_draw_line(l, &d);
          };
          auto tri = [](lv_layer_t *l, float ax, float ay, float bx, float by, float cx, float cy,
                        uint32_t c, lv_opa_t o, uint32_t c2 = 0, lv_opa_t o2 = 0) {
            lv_draw_triangle_dsc_t d; lv_draw_triangle_dsc_init(&d);
            d.p[0].x = ax; d.p[0].y = ay; d.p[1].x = bx; d.p[1].y = by; d.p[2].x = cx; d.p[2].y = cy;
            d.color = lv_color_hex(c); d.opa = o;
            if (o2) {
              d.grad.dir = LV_GRAD_DIR_VER; d.grad.stops_count = 2;
              d.grad.stops[0].color = lv_color_hex(c);  d.grad.stops[0].opa = o;  d.grad.stops[0].frac = 0;
              d.grad.stops[1].color = lv_color_hex(c2); d.grad.stops[1].opa = o2; d.grad.stops[1].frac = 255;
            }
            lv_draw_triangle(l, &d);
          };
          auto dot = [](lv_layer_t *l, float x, float y, int r, uint32_t c) {
            lv_draw_rect_dsc_t d; lv_draw_rect_dsc_init(&d);
            d.bg_color = lv_color_hex(c); d.bg_opa = LV_OPA_COVER; d.radius = LV_RADIUS_CIRCLE;
            d.border_color = lv_color_hex(0xffffff); d.border_width = 1;
            lv_area_t a = {(int32_t)(x - r), (int32_t)(y - r), (int32_t)(x + r), (int32_t)(y + r)};
            lv_draw_rect(l, &d, &a);
          };

          // ---------- радар сети, холст 160x88, центр (62,44), r=28 ----------
          {
            lv_obj_t *cv = (lv_obj_t *) id(radar_cv);
            lv_canvas_fill_bg(cv, lv_color_hex(0x0d1433), LV_OPA_COVER);
            lv_layer_t l; lv_canvas_init_layer(cv, &l);
            const float cx = 62, cy = 44, R = 28;
            const int N = 6;
            const auto &v = id(g_radar);   // 0..1: U, f, cos, I, нагрев розетки, плата счетчика
            float ax[N], ay[N];
            for (int i = 0; i < N; i++) {
              float a = -M_PI / 2 + i * 2 * M_PI / N;
              ax[i] = cosf(a); ay[i] = sinf(a);
            }
            for (int ring = 1; ring <= 4; ring++) {
              float r = R * ring / 4;
              for (int i = 0; i < N; i++) {
                int j = (i + 1) % N;
                line(&l, cx + ax[i] * r, cy + ay[i] * r, cx + ax[j] * r, cy + ay[j] * r, 0x3a4a86, 1, LV_OPA_70);
              }
            }
            for (int i = 0; i < N; i++) line(&l, cx, cy, cx + ax[i] * R, cy + ay[i] * R, 0x3a4a86, 1, LV_OPA_70);
            // три слоя заливки дают переход от оранжевого в центре к малиновому по краю
            const float sc[3] = {1.0f, 0.72f, 0.42f};
            const uint32_t col[3] = {0xd63fe0, 0xff5f8f, 0xffa040};
            for (int k = 0; k < 3; k++)
              for (int i = 0; i < N; i++) {
                int j = (i + 1) % N;
                float ri = R * v[i] * sc[k], rj = R * v[j] * sc[k];
                tri(&l, cx, cy, cx + ax[i] * ri, cy + ay[i] * ri, cx + ax[j] * rj, cy + ay[j] * rj, col[k], LV_OPA_90);
              }
            for (int i = 0; i < N; i++) {
              int j = (i + 1) % N;
              line(&l, cx + ax[i] * R * v[i], cy + ay[i] * R * v[i], cx + ax[j] * R * v[j], cy + ay[j] * R * v[j], 0xffd0f0, 1, LV_OPA_COVER);
            }
            for (int i = 0; i < N; i++) dot(&l, cx + ax[i] * R * v[i], cy + ay[i] * R * v[i], 2, 0xff7ab8);
            lv_canvas_finish_layer(cv, &l);
          }

          // ---------- график, холст 224x60: сутки по часам / неделя / месяц по дням ----------
          {
            lv_obj_t *cv = (lv_obj_t *) id(area_cv);
            lv_canvas_fill_bg(cv, lv_color_hex(0x0c1230), LV_OPA_COVER);
            lv_layer_t l; lv_canvas_init_layer(cv, &l);
            const int view = id(g_view);
            std::vector<float> v;
            if (view == 0) v = id(g_power);
            else {
              const auto &d = id(g_days);
              int n = view == 1 ? 7 : 30;
              int from = std::max(0, (int) d.size() - n);
              v.assign(d.begin() + from, d.end());
            }
            const int N = v.size();
            const float x0 = 4, bot = 58, top = 8;
            const float dx = N > 1 ? 216.0f / (N - 1) : 0;
            float vmax = view == 0 ? 0.5f : 1.0f;
            for (float x : v) vmax = std::max(vmax, x * 1.15f);
            for (int g = 1; g <= 4; g++) {
              float y = bot - (bot - top) * g / 5;
              line(&l, 0, y, 224, y, 0x26325f, 1, LV_OPA_60);
            }
            std::vector<float> px(N), py(N);
            for (int i = 0; i < N; i++) { px[i] = x0 + i * dx; py[i] = bot - (bot - top) * v[i] / vmax; }
            for (int i = 0; i + 1 < N; i++) {
              tri(&l, px[i], py[i], px[i + 1], py[i + 1], px[i], bot, 0x9b6bff, LV_OPA_90, 0x2a1a70, LV_OPA_50);
              tri(&l, px[i + 1], py[i + 1], px[i + 1], bot, px[i], bot, 0x9b6bff, LV_OPA_90, 0x2a1a70, LV_OPA_50);
            }
            for (int i = 0; i + 1 < N; i++) line(&l, px[i], py[i], px[i + 1], py[i + 1], 0xc9b8ff, 2, LV_OPA_COVER);
            if (view == 1) for (int i = 0; i < N; i++) dot(&l, px[i], py[i], 2, 0x8a5cff);
            // подписи: максимум и последняя точка, на подложке поверх сетки
            lv_obj_t *lab[2] = {(lv_obj_t *) id(l_amax), (lv_obj_t *) id(l_anow)};
            int imax = 0;
            for (int i = 1; i < N; i++) if (v[i] > v[imax]) imax = i;
            int idx[2] = {imax, N - 1};
            for (int k = 0; k < 2; k++) {
              if (N < 2 || (k == 0 && imax == N - 1)) { lv_label_set_text(lab[k], ""); continue; }
              int i = idx[k];
              line(&l, px[i], py[i], px[i], bot, 0xc9b8ff, 1, LV_OPA_50);
              dot(&l, px[i], py[i], 3, 0x8a5cff);
              char b[16];
              if (view == 0) snprintf(b, sizeof b, v[i] >= 1 ? "%.2f" : "%.0f", v[i] >= 1 ? v[i] : v[i] * 1000);
              else snprintf(b, sizeof b, v[i] >= 10 ? "%.0f" : "%.1f", v[i]);
              lv_label_set_text(lab[k], b);
              int lx = 6 + (int) px[i] - 14, ly = 20 + (int) py[i] - 13;
              lx = std::max(0, std::min(lx, 236 - 30));
              ly = std::max(16, ly);
              lv_obj_set_pos(lab[k], lx, ly);
            }
            // подписи под графиком: 7 мест
            static const char *DOW[7] = {"Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"};
            lv_obj_t *hl[7] = {(lv_obj_t *) id(h0), (lv_obj_t *) id(h1), (lv_obj_t *) id(h2), (lv_obj_t *) id(h3),
                               (lv_obj_t *) id(h4), (lv_obj_t *) id(h5), (lv_obj_t *) id(h6)};
            const int dn = id(g_days).size();
            for (int k = 0; k < 7; k++) {
              int i;
              char b[8] = "";
              if (view == 0) {
                i = k < 6 ? k * 4 : N - 1;
                if (N >= 2 && i < N) snprintf(b, sizeof b, "%d", (id(g_first_hour) + i + (k == 6 ? 1 : 0)) % 24);
              } else if (view == 1) {
                i = k;
                int day = dn - N + i;   // индекс дня в g_days
                if (i < N) snprintf(b, sizeof b, "%s", DOW[((id(g_first_dow) + day) % 7 + 7) % 7]);
              } else {
                i = k < 6 ? k * 5 : N - 1;
                if (N >= 2 && i < N) {
                  // число месяца: от первого дня g_days вперед, с переходом через 28-31
                  int day = dn - N + i;
                  ESPTime t = id(ha_time).now();
                  int num = 0;
                  if (t.is_valid()) {
                    time_t ts = t.timestamp - (time_t) (dn - 1 - day) * 86400;
                    struct tm tmv;
                    localtime_r(&ts, &tmv);
                    num = tmv.tm_mday;
                  }
                  snprintf(b, sizeof b, "%d", num);
                }
              }
              lv_label_set_text(hl[k], b);
              if (b[0] && i < N) lv_obj_set_x(hl[k], 6 + (int) px[i] - 10);
            }
            lv_canvas_finish_layer(cv, &l);
          }

  # Раскладывает текущие значения по виджетам. Текст и значения ставятся только
  # при изменении, иначе LVGL перерисовывал бы панели каждые 2 секунды.
  - id: refresh
    then:
      - lambda: |-
          auto set = [](lv_obj_t *o, const char *t) {
            if (strcmp(lv_label_get_text(o), t) != 0) lv_label_set_text(o, t);
          };
          auto num = [&](lv_obj_t *o, const char *f, float v) {
            char b[32];
            if (std::isnan(v)) snprintf(b, sizeof b, "--"); else snprintf(b, sizeof b, f, v);
            set(o, b);
          };
          auto bar = [](lv_obj_t *o, int32_t lo, int32_t hi, float v) {
            int32_t x = std::isnan(v) ? lo : (int32_t) lroundf(v);
            if (x < lo) x = lo;
            if (x > hi) x = hi;
            if (lv_bar_get_min_value(o) != lo || lv_bar_get_max_value(o) != hi) lv_bar_set_range(o, lo, hi);
            if (lv_bar_get_value(o) != x) lv_bar_set_value(o, x, LV_ANIM_OFF);
          };
          auto chip = [](lv_obj_t *o, bool on, uint32_t c) {
            lv_color_t bg = lv_color_hex(on ? c : 0x18224a);
            lv_color_t fg = lv_color_hex(on ? 0xffffff : 0x8ea2d8);
            if (!lv_color_eq(lv_obj_get_style_bg_color(o, LV_PART_MAIN), bg)) {
              lv_obj_set_style_bg_color(o, bg, LV_PART_MAIN);
              lv_obj_set_style_text_color(o, fg, LV_PART_MAIN);
            }
          };
          auto z = [](float v) { return std::isnan(v) ? 0.0f : v; };

          // ---- мощность сейчас
          float p = id(ha_p_now).state;
          if (!std::isnan(p) && p >= 1000) { num(((lv_obj_t *) id(l_pnow)), "%.2f", p / 1000); set(((lv_obj_t *) id(l_punit)), "кВт"); }
          else { num(((lv_obj_t *) id(l_pnow)), "%.0f", p); set(((lv_obj_t *) id(l_punit)), "Вт"); }
          lv_obj_align_to(((lv_obj_t *) id(l_punit)), ((lv_obj_t *) id(l_pnow)), LV_ALIGN_OUT_RIGHT_BOTTOM, 4, -5);
          bool on_sockets = id(ha_line).state == "Розетки гаража";
          float p_line = on_sockets ? z(id(ha_p_sock).state) : z(id(ha_p_chg).state);
          bool charging = p_line > 300;
          set(((lv_obj_t *) id(l_psub)), charging ? "заряжается авто" : "потребление");

          // ---- сегодня
          float ds = id(ha_d_sock).state, dl = id(ha_d_light).state, dc = id(ha_d_chg).state;
          num(((lv_obj_t *) id(l_d_sock)), "%.1f", ds);
          num(((lv_obj_t *) id(l_d_light)), "%.1f", dl);
          num(((lv_obj_t *) id(l_d_chg)), "%.1f", dc);
          num(((lv_obj_t *) id(l_d_all)), "%.1f", z(ds) + z(dl) + z(dc));

          // ---- зарядка авто: сессия выбранной линии и окно
          float sess = on_sockets ? id(ha_s_sock).state : id(ha_s_chg).state;
          char b[40];
          if (std::isnan(sess)) snprintf(b, sizeof b, "-- кВт·ч"); else snprintf(b, sizeof b, "%.1f кВт·ч", sess);
          set(((lv_obj_t *) id(l_sess)), b);
          int sh = atoi(id(ha_chg_start).state.c_str()), sm = 0, eh = atoi(id(ha_chg_end).state.c_str()), em = 0;
          sscanf(id(ha_chg_start).state.c_str(), "%d:%d", &sh, &sm);
          sscanf(id(ha_chg_end).state.c_str(), "%d:%d", &eh, &em);
          snprintf(b, sizeof b, "%02d-%02d", sh, eh);
          set(((lv_obj_t *) id(l_win)), b);
          int pct = 0;
          auto now = id(ha_time).now();
          if (now.is_valid() && id(ha_chg_on).state == "on") {
            int s0 = sh * 60 + sm, e0 = eh * 60 + em, n0 = now.hour * 60 + now.minute;
            int len = (e0 - s0 + 1440) % 1440, pos = (n0 - s0 + 1440) % 1440;
            if (len > 0 && pos < len) pct = pos * 100 / len;
          }
          bar(((lv_obj_t *) id(b_win)), 0, 100, pct);
          snprintf(b, sizeof b, id(ha_chg_on).state == "on" ? "%d%%" : "выкл", pct);
          set(((lv_obj_t *) id(l_winpct)), b);

          // ---- фишки
          chip(((lv_obj_t *) id(c_gate)), id(ha_gate).has_state() && id(ha_gate).state, 0xff7a1a);
          const std::string &al = id(ha_alarm).state;
          bool armed = al.rfind("armed", 0) == 0, trig = al == "triggered";
          chip(((lv_obj_t *) id(c_alarm)), armed || trig, trig ? 0xff2d55 : 0x1e6bff);
          chip(((lv_obj_t *) id(c_light)), id(ha_light).state == "on", 0xd99a00);
          chip(((lv_obj_t *) id(c_motion)), id(ha_motion).has_state() && id(ha_motion).state, 0xb45cff);

          // ---- месяц и кольцевая
          static const char *MON[12] = {"ЯНВАРЬ", "ФЕВРАЛЬ", "МАРТ", "АПРЕЛЬ", "МАЙ", "ИЮНЬ",
                                        "ИЮЛЬ", "АВГУСТ", "СЕНТЯБРЬ", "ОКТЯБРЬ", "НОЯБРЬ", "ДЕКАБРЬ"};
          if (now.is_valid()) set(((lv_obj_t *) id(l_month)), MON[now.month - 1]);
          float ms = z(id(ha_m_sock).state), ml = z(id(ha_m_light).state), mc = z(id(ha_m_chg).state);
          float mt = ms + ml + mc;
          // итог месяца со счетчика ATORCH (он с 10.09.2026), учет по линиям моложе
          // (с 22.09.2026) и дает только доли в кольцевой
          const auto &MM = id(g_months);
          float matorch = (now.is_valid() && (int) MM.size() >= now.month) ? MM[now.month - 1] : 0;
          float mshow = matorch > 0 ? matorch : mt;
          snprintf(b, sizeof b, "%.1f кВт·ч", mshow); set(((lv_obj_t *) id(l_mtot)), b);
          num(((lv_obj_t *) id(l_dtot)), mshow >= 100 ? "%.0f" : "%.1f", mshow);
          float fr[3] = {mt > 0 ? ms / mt : 0, mt > 0 ? ml / mt : 0, mt > 0 ? mc / mt : 0};
          lv_obj_t *arcs[3] = {((lv_obj_t *) id(a_sock)), ((lv_obj_t *) id(a_light)), ((lv_obj_t *) id(a_chg))};
          lv_obj_t *pcts[3] = {((lv_obj_t *) id(l_pc_sock)), ((lv_obj_t *) id(l_pc_light)), ((lv_obj_t *) id(l_pc_chg))};
          float a0 = 0;
          for (int i = 0; i < 3; i++) {
            float span = fr[i] * 360;
            int st = (int) lroundf(a0), en = (int) lroundf(a0 + span) - (span > 6 ? 3 : 0);
            if (span < 1) { st = 0; en = 0; }
            if (lv_arc_get_bg_angle_start(arcs[i]) != st || lv_arc_get_bg_angle_end(arcs[i]) != en)
              lv_arc_set_bg_angles(arcs[i], st, en);
            a0 += span;
            snprintf(b, sizeof b, "%.0f%%", fr[i] * 100); set(pcts[i], b);
          }

          // ---- сеть: радар раз в 10 секунд, подпись напряжения сразу
          float U = id(ha_volt).state, F = id(ha_freq).state, PF = id(ha_pf).state, I = id(ha_cur).state;
          snprintf(b, sizeof b, std::isnan(U) ? "-- В" : "%.0f В", U); set(((lv_obj_t *) id(l_volt)), b);
          auto cl = [](float v) { return std::isnan(v) ? 0.0f : std::min(1.0f, std::max(0.05f, v)); };
          std::vector<float> r = {cl((U - 200) / 50), cl((F - 49) / 2), cl(PF / 100), cl(I / 16),
                                  cl(id(ha_sock_t).state / 60), cl(id(ha_cpu_t).state / 60)};
          if (++id(g_tick) % 5 == 0 && r != id(g_radar)) {
            id(g_radar) = r;
            id(draw_charts).execute();
          }

          // ---- климат
          float t = id(ha_temp).state, h = id(ha_hum).state, pr = id(ha_press).state, st = id(ha_sock_t).state;
          num(((lv_obj_t *) id(l_temp)), "%.1f°", t);  bar(((lv_obj_t *) id(b_temp)), 0, 100, (t + 5) * 2);
          num(((lv_obj_t *) id(l_hum)), "%.0f%%", h);  bar(((lv_obj_t *) id(b_hum)), 0, 100, h);
          num(((lv_obj_t *) id(l_press)), "%.0f гПа", pr); bar(((lv_obj_t *) id(b_press)), 0, 100, pr - 950);
          num(((lv_obj_t *) id(l_sockt)), "%.0f°", st); bar(((lv_obj_t *) id(b_sockt)), 0, 100, st * 100 / 60);
          float bm = NAN;
          for (float v : {id(ha_bat1).state, id(ha_bat2).state, id(ha_bat3).state})
            if (!std::isnan(v) && (std::isnan(bm) || v < bm)) bm = v;
          num(((lv_obj_t *) id(l_bat)), "%.0f%%", bm); bar(((lv_obj_t *) id(b_bat)), 0, 100, bm);

          // ---- вкладки графика
          int view = id(g_view);
          set(((lv_obj_t *) id(l_atitle)), view == 0 ? "МОЩНОСТЬ, кВт" : "ЭНЕРГИЯ, кВт·ч");
          chip(((lv_obj_t *) id(t_day)), view == 0, 0x1e6bff);
          chip(((lv_obj_t *) id(t_week)), view == 1, 0x1e6bff);
          chip(((lv_obj_t *) id(t_month)), view == 2, 0x1e6bff);

          // ---- столбики по месяцам
          lv_obj_t *mb[12] = {((lv_obj_t *) id(m1)), ((lv_obj_t *) id(m2)), ((lv_obj_t *) id(m3)), ((lv_obj_t *) id(m4)), ((lv_obj_t *) id(m5)), ((lv_obj_t *) id(m6)), ((lv_obj_t *) id(m7)), ((lv_obj_t *) id(m8)), ((lv_obj_t *) id(m9)), ((lv_obj_t *) id(m10)), ((lv_obj_t *) id(m11)), ((lv_obj_t *) id(m12))};
          lv_obj_t *mc_[12] = {((lv_obj_t *) id(k1)), ((lv_obj_t *) id(k2)), ((lv_obj_t *) id(k3)), ((lv_obj_t *) id(k4)), ((lv_obj_t *) id(k5)), ((lv_obj_t *) id(k6)), ((lv_obj_t *) id(k7)), ((lv_obj_t *) id(k8)), ((lv_obj_t *) id(k9)), ((lv_obj_t *) id(k10)), ((lv_obj_t *) id(k11)), ((lv_obj_t *) id(k12))};
          auto &M = id(g_months);
          float mmax = 10;
          for (float v : M) mmax = std::max(mmax, v * 1.1f);
          int cur = now.is_valid() ? now.month - 1 : -1;
          for (int i = 0; i < 12; i++) {
            float v = i < (int) M.size() ? M[i] : 0;
            bar(mb[i], 0, (int32_t) (mmax * 10), v * 10);
            chip(mc_[i], i == cur, 0x1e6bff);
          }
          // подписи над текущим и двумя предыдущими месяцами, если есть данные
          lv_obj_t *mv[3] = {((lv_obj_t *) id(mv0)), ((lv_obj_t *) id(mv1)), ((lv_obj_t *) id(mv2))};
          for (int k = 0; k < 3; k++) {
            int i = cur - 2 + k;
            float v = (i >= 0 && i < (int) M.size()) ? M[i] : 0;
            if (i < 0 || v <= 0) { set(mv[k], ""); continue; }
            snprintf(b, sizeof b, v >= 10 ? "%.0f" : "%.1f", v); set(mv[k], b);
            int hpx = (int) (58 * v / mmax);
            lv_obj_set_pos(mv[k], 12 + i * 18 - 5, 18 + 58 - hpx - 10);
          }

lvgl:
  id: lvgl_comp
  displays: [lcd]
  rotation: 180
  touchscreens: [ts]
  buffer_size: 25%
  default_font: fs
  theme:
    obj:
      pad_all: 0
      border_width: 0
      radius: 0
      bg_opa: TRANSP
      scrollbar_mode: "OFF"
      scrollable: false
    label:
      text_color: 0xe6ecff
    bar:
      pad_all: 0
      scrollable: false
  style_definitions:
    - id: panel
      bg_opa: COVER
      bg_color: 0x0f1636
      bg_grad_color: 0x0a0f26
      bg_grad_dir: VER
      radius: 10
      border_width: 1
      border_color: 0x27366e
    - id: glow_blue
      border_width: 2
      border_color: 0x4a9bff
      shadow_color: 0x2f7bff
      shadow_width: 12
      shadow_opa: 70%
    - id: glow_violet
      border_width: 2
      border_color: 0xb45cff
      shadow_color: 0x9b30ff
      shadow_width: 12
      shadow_opa: 70%
    - id: dim
      text_color: 0x8ea2d8
    # подложка подписей на графике: закрывает линии сетки под текстом
    - id: plate
      bg_opa: COVER
      bg_color: 0x0c1230
      radius: 2
      pad_top: 1
      pad_bottom: 1
    - id: chip
      bg_opa: COVER
      bg_color: 0x18224a
      radius: 3
      text_color: 0x8ea2d8
      text_align: CENTER
    - id: chip_on
      bg_opa: COVER
      bg_color: 0x1e6bff
      radius: 3
      text_color: 0xffffff
      text_align: CENTER
  pages:
    - id: dash
      bg_opa: COVER
      bg_color: 0x050918
      bg_grad_color: 0x0c1438
      bg_grad_dir: VER
      scrollable: false
      widgets:
        # ================= ряд 1 =================
        - obj:
            x: 4
            y: 4
            width: 112
            height: 64
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            bg_color: 0x2f86ff
            bg_grad_color: 0x1238b8
            bg_grad_dir: HOR
            radius: 12
            border_width: 0
            shadow_color: 0x2f7bff
            shadow_width: 14
            shadow_opa: 70%
            widgets:
              - label: {x: 8, y: 5, text: "ГАРАЖ СЕЙЧАС", text_font: fs}
              - label: {id: l_pnow, x: 7, y: 15, text: "--", text_font: fx}
              - label: {id: l_punit, x: 76, y: 32, text: "Вт", text_font: fm}
              - label: {id: l_psub, x: 8, y: 52, text: "", text_font: fs, text_color: 0xcfe0ff}

        - obj:
            x: 120
            y: 4
            width: 120
            height: 64
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              - label: {x: 0, y: 4, width: 120, text: "СЕГОДНЯ, кВт·ч", text_font: fs, text_align: CENTER, text_color: 0xaab8f0}
              - label: {x: 10, y: 17, text: "Розетки", text_font: fs, styles: [dim]}
              - label: {id: l_d_sock, x: 70, y: 17, width: 40, text: "--", text_font: fs, text_align: RIGHT}
              - label: {x: 10, y: 28, text: "Свет", text_font: fs, styles: [dim]}
              - label: {id: l_d_light, x: 70, y: 28, width: 40, text: "--", text_font: fs, text_align: RIGHT}
              - label: {x: 10, y: 39, text: "Зарядка", text_font: fs, styles: [dim]}
              - label: {id: l_d_chg, x: 70, y: 39, width: 40, text: "--", text_font: fs, text_align: RIGHT}
              - obj: {x: 10, y: 49, width: 100, height: 1, bg_opa: COVER, bg_color: 0x27366e}
              - label: {x: 10, y: 52, text: "ВСЕГО", text_font: fs}
              - label: {id: l_d_all, x: 70, y: 52, width: 40, text: "--", text_font: fs, text_align: RIGHT}

        - obj:
            x: 244
            y: 4
            width: 120
            height: 40
            styles: [panel, glow_violet]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              - label: {x: 0, y: 5, width: 116, text: "ЗАРЯДКА АВТО", text_font: fs, text_align: CENTER, text_color: 0xaab8f0}
              - label: {id: l_sess, x: 0, y: 17, width: 116, text: "--", text_font: fb, text_align: CENTER}

        - obj:
            x: 244
            y: 48
            width: 120
            height: 20
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            radius: 8
            widgets:
              - label: {id: l_win, x: 6, y: 6, text: "--", text_font: fs, styles: [dim]}
              - bar:
                  id: b_win
                  x: 40
                  y: 6
                  width: 74
                  height: 8
                  min_value: 0
                  max_value: 100
                  value: 0
                  bg_opa: COVER
                  bg_color: 0x1a2350
                  radius: 4
                  indicator:
                    bg_color: 0xff9a3c
                    bg_grad_color: 0xe040fb
                    bg_grad_dir: HOR
                    radius: 4
              - label: {id: l_winpct, x: 40, y: 6, width: 74, text: "", text_font: fs, text_align: CENTER}

        - obj:
            x: 368
            y: 4
            width: 108
            height: 64
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              - label: {id: c_gate, x: 5, y: 5, width: 47, height: 12, text: "ВОРОТА", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: c_alarm, x: 56, y: 5, width: 47, height: 12, text: "ОХРАНА", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: c_light, x: 5, y: 20, width: 47, height: 12, text: "СВЕТ", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: c_motion, x: 56, y: 20, width: 47, height: 12, text: "ДВИЖ.", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: l_month, x: 0, y: 36, width: 108, text: "", text_font: fm, text_align: CENTER}
              - label: {id: l_mtot, x: 0, y: 52, width: 102, text: "--", text_font: fs, text_align: RIGHT, styles: [dim]}

        # ================= ряд 2 =================
        - obj:
            x: 4
            y: 72
            width: 128
            height: 92
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              # кольцевая из трех дуг, 0 градусов сверху (rotation 270)
              - arc: &seg
                  id: a_sock
                  x: 6
                  y: 14
                  width: 64
                  height: 64
                  rotation: 270
                  start_angle: 0
                  end_angle: 0
                  adjustable: false
                  clickable: false
                  arc_width: 9
                  arc_color: 0xff9a3c
                  arc_rounded: false
                  indicator:
                    arc_opa: TRANSP
                  knob:
                    bg_opa: TRANSP
                    pad_all: 0
              - arc:
                  <<: *seg
                  id: a_light
                  start_angle: 0
                  end_angle: 0
                  arc_color: 0xe040fb
              - arc:
                  <<: *seg
                  id: a_chg
                  start_angle: 0
                  end_angle: 0
                  arc_color: 0x3f8cff
              - label: {id: l_dtot, x: 6, y: 35, width: 64, text: "--", text_font: fb, text_align: CENTER}
              - label: {x: 6, y: 51, width: 64, text: "кВт·ч", text_font: fs, text_align: CENTER, styles: [dim]}
              - obj: {x: 76, y: 14, width: 7, height: 7, bg_opa: COVER, bg_color: 0xff9a3c, radius: 2}
              - label: {id: l_pc_sock, x: 86, y: 14, text: "", text_font: fs}
              - label: {x: 76, y: 24, text: "Розетки", text_font: fs, styles: [dim]}
              - obj: {x: 76, y: 38, width: 7, height: 7, bg_opa: COVER, bg_color: 0xe040fb, radius: 2}
              - label: {id: l_pc_light, x: 86, y: 38, text: "", text_font: fs}
              - label: {x: 76, y: 48, text: "Свет", text_font: fs, styles: [dim]}
              - obj: {x: 76, y: 62, width: 7, height: 7, bg_opa: COVER, bg_color: 0x3f8cff, radius: 2}
              - label: {id: l_pc_chg, x: 86, y: 62, text: "", text_font: fs}
              - label: {x: 76, y: 72, text: "Зарядка", text_font: fs, styles: [dim]}

        - obj:
            x: 136
            y: 72
            width: 164
            height: 92
            styles: [panel, glow_violet]
            scrollbar_mode: "OFF"
            scrollable: false
            bg_color: 0x0d1433
            bg_grad_dir: NONE
            widgets:
              - canvas:
                  id: radar_cv
                  x: 2
                  y: 2
                  width: 160
                  height: 88
              # подписи осей: центр радара в координатах панели (64,46), r=28
              - label: {x: 34, y: 4, width: 60, text: "Напряж.", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {x: 91, y: 26, text: "Частота", text_font: fs, styles: [dim]}
              - label: {x: 91, y: 58, text: "Косинус", text_font: fs, styles: [dim]}
              - label: {x: 34, y: 80, width: 60, text: "Ток", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {x: 0, y: 58, width: 37, text: "Плата", text_font: fs, text_align: RIGHT, styles: [dim]}
              - label: {x: 0, y: 26, width: 37, text: "Нагрев", text_font: fs, text_align: RIGHT, styles: [dim]}
              - label: {x: 118, y: 5, width: 40, text: "СЕТЬ", text_font: fs, text_align: RIGHT, text_color: 0xaab8f0}
              - label: {id: l_volt, x: 110, y: 72, width: 48, text: "--", text_font: fb, text_align: RIGHT}

        - obj:
            x: 304
            y: 72
            width: 172
            height: 92
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              - label: {x: 0, y: 4, width: 164, text: "КЛИМАТ", text_font: fs, text_align: RIGHT, text_color: 0xaab8f0}
              - label: {x: 8, y: 14, text: "Температура", text_font: fs}
              - label: {id: l_temp, x: 110, y: 14, width: 54, text: "--", text_font: fs, text_align: RIGHT}
              - bar: &hbar
                  id: b_temp
                  x: 8
                  y: 24
                  width: 156
                  height: 3
                  min_value: 0
                  max_value: 100
                  value: 0
                  bg_opa: COVER
                  bg_color: 0x1a2350
                  radius: 2
                  indicator:
                    bg_color: 0xff9a3c
                    bg_grad_color: 0xe040fb
                    bg_grad_dir: HOR
                    radius: 2
              - label: {x: 8, y: 29, text: "Влажность", text_font: fs}
              - label: {id: l_hum, x: 110, y: 29, width: 54, text: "--", text_font: fs, text_align: RIGHT}
              - bar: {<<: *hbar, id: b_hum, y: 39}
              - label: {x: 8, y: 44, text: "Давление", text_font: fs}
              - label: {id: l_press, x: 110, y: 44, width: 54, text: "--", text_font: fs, text_align: RIGHT}
              - bar: {<<: *hbar, id: b_press, y: 54}
              - label: {x: 8, y: 59, text: "Нагрев розетки", text_font: fs}
              - label: {id: l_sockt, x: 110, y: 59, width: 54, text: "--", text_font: fs, text_align: RIGHT}
              - bar: {<<: *hbar, id: b_sockt, y: 69}
              - label: {x: 8, y: 74, text: "Батарейки мин.", text_font: fs}
              - label: {id: l_bat, x: 110, y: 74, width: 54, text: "--", text_font: fs, text_align: RIGHT}
              - bar: {<<: *hbar, id: b_bat, y: 84}

        # ================= ряд 3 =================
        - obj:
            x: 4
            y: 168
            width: 232
            height: 100
            styles: [panel]
            scrollbar_mode: "OFF"
            scrollable: false
            widgets:
              - label: {x: 8, y: 5, text: "ГАРАЖ ПО МЕСЯЦАМ, кВт·ч", text_font: fs, styles: [dim]}
              - bar: &vbar
                  id: m1
                  x: 12
                  y: 18
                  width: 10
                  height: 58
                  min_value: 0
                  max_value: 100
                  value: 0
                  bg_opa: COVER
                  bg_color: 0x151d42
                  radius: 3
                  indicator:
                    bg_color: 0xe040fb
                    bg_grad_color: 0xff9a3c
                    bg_grad_dir: VER
                    radius: 3
              - bar: {<<: *vbar, id: m2, x: 30}
              - bar: {<<: *vbar, id: m3, x: 48}
              - bar: {<<: *vbar, id: m4, x: 66}
              - bar: {<<: *vbar, id: m5, x: 84}
              - bar: {<<: *vbar, id: m6, x: 102}
              - bar: {<<: *vbar, id: m7, x: 120}
              - bar: {<<: *vbar, id: m8, x: 138}
              - bar: {<<: *vbar, id: m9, x: 156}
              - bar: {<<: *vbar, id: m10, x: 174}
              - bar: {<<: *vbar, id: m11, x: 192}
              - bar: {<<: *vbar, id: m12, x: 210}
              - label: {id: mv0, x: 0, y: 0, width: 20, text: "", text_font: fs, text_align: CENTER}
              - label: {id: mv1, x: 0, y: 0, width: 20, text: "", text_font: fs, text_align: CENTER}
              - label: {id: mv2, x: 0, y: 0, width: 20, text: "", text_font: fs, text_align: CENTER}
              - label: {id: k1, x: 10, y: 82, width: 14, height: 11, text: "Я", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k2, x: 28, y: 82, width: 14, height: 11, text: "Ф", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k3, x: 46, y: 82, width: 14, height: 11, text: "М", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k4, x: 64, y: 82, width: 14, height: 11, text: "А", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k5, x: 82, y: 82, width: 14, height: 11, text: "М", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k6, x: 100, y: 82, width: 14, height: 11, text: "И", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k7, x: 118, y: 82, width: 14, height: 11, text: "И", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k8, x: 136, y: 82, width: 14, height: 11, text: "А", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k9, x: 154, y: 82, width: 14, height: 11, text: "С", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k10, x: 172, y: 82, width: 14, height: 11, text: "О", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k11, x: 190, y: 82, width: 14, height: 11, text: "Н", text_font: fs, pad_top: 2, styles: [chip]}
              - label: {id: k12, x: 208, y: 82, width: 14, height: 11, text: "Д", text_font: fs, pad_top: 2, styles: [chip]}

        - obj:
            x: 240
            y: 168
            width: 236
            height: 100
            styles: [panel, glow_blue]
            scrollbar_mode: "OFF"
            scrollable: false
            bg_color: 0x0c1230
            bg_grad_dir: NONE
            widgets:
              - label: {id: l_atitle, x: 8, y: 5, text: "", text_font: fs, text_color: 0xaab8f0}
              - label:
                  id: t_day
                  x: 96
                  y: 3
                  width: 38
                  height: 12
                  text: "СУТКИ"
                  text_font: fs
                  pad_top: 2
                  clickable: true
                  styles: [chip]
                  on_click:
                    - lambda: |-
                        id(g_view) = 0;
                        ESP_LOGI("dash", "tap: view 0");
                    - script.execute: draw_charts
                    - script.execute: refresh
              - label:
                  id: t_week
                  x: 137
                  y: 3
                  width: 44
                  height: 12
                  text: "НЕДЕЛЯ"
                  text_font: fs
                  pad_top: 2
                  clickable: true
                  styles: [chip]
                  on_click:
                    - lambda: |-
                        id(g_view) = 1;
                        ESP_LOGI("dash", "tap: view 1");
                    - script.execute: draw_charts
                    - script.execute: refresh
              - label:
                  id: t_month
                  x: 184
                  y: 3
                  width: 42
                  height: 12
                  text: "МЕСЯЦ"
                  text_font: fs
                  pad_top: 2
                  clickable: true
                  styles: [chip]
                  on_click:
                    - lambda: |-
                        id(g_view) = 2;
                        ESP_LOGI("dash", "tap: view 2");
                    - script.execute: draw_charts
                    - script.execute: refresh
              - canvas:
                  id: area_cv
                  x: 6
                  y: 20
                  width: 224
                  height: 60
              - label: {id: l_amax, x: 0, y: 20, width: 28, text: "", text_font: fs, text_align: CENTER, styles: [plate]}
              - label: {id: l_anow, x: 0, y: 20, width: 28, text: "", text_font: fs, text_align: CENTER, styles: [plate]}
              - label: {id: h0, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h1, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h2, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h3, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h4, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h5, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
              - label: {id: h6, x: 0, y: 85, width: 20, text: "", text_font: fs, text_align: CENTER, styles: [dim]}
